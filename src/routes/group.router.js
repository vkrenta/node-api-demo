import { Router } from 'express';

const groupRouter = Router();

groupRouter.post('/create', () => {});
groupRouter.get('/all', () => {});
groupRouter.get('/:id', () => {});

export default groupRouter;
